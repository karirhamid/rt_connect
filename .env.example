# ZKTeco device connection defaults
# (Real devices are added in the web admin → Devices page.
#  These are only fallbacks for legacy code paths.)
DEVICE_TIMEOUT=30
DEVICE_PASSWORD=0

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_TITLE="ZKTeco Device Management API"
API_VERSION=1.0.0

# Database
DATABASE_URL=sqlite+aiosqlite:///./zkteco.db
