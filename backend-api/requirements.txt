fastapi==0.104.1
uvicorn[standard]==0.24.0
pyzk==0.9
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
reportlab==3.6.12

# Auth: password hashing + JWT
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
bcrypt==4.0.1

# Device discovery / outbound HTTP
requests==2.31.0

# External backup storage — SMB / network shares (Windows, Samba on Linux)
smbprotocol==1.13.0

# Payroll exports (XLSX)
openpyxl==3.1.2

# Timezone database (needed by zoneinfo on Windows / slim images)
tzdata==2024.1
